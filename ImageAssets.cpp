#include "ReadImage.h" // Read image into pixel vector array -> ReadImage::ReadPNG(const char* filename, int &width, int &height) ;
#include "Image.h" // Draw image from pixel vector array -> Image::DrawImage(const std::vector<std::vector<unsigned int>>& pixels, int offsetX, int offsetY);
// Experimental ~ coded to learn, with guesswork by Kieran J
